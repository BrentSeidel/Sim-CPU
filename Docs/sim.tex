\documentclass[10pt, openany]{book}
%
%  Packages to use
%
\usepackage{fancyhdr}
\usepackage{fancyvrb}
\usepackage{fancybox}
%
\usepackage{lastpage}
\usepackage{imakeidx}
%
\usepackage{amsmath}
\usepackage{amsfonts}
%
\usepackage{geometry}
\geometry{letterpaper}
%
\usepackage{url}
\usepackage{gensymb}
\usepackage{multicol}
\usepackage{xcolor}
%
\usepackage[pdf]{pstricks}
\usepackage{graphicx}
\DeclareGraphicsExtensions{.pdf}
\DeclareGraphicsRule{.pdf}{pdf}{.pdf}{}
%
% Rules to allow import of graphics files in EPS format
%
\usepackage{graphicx}
\DeclareGraphicsExtensions{.eps}
\DeclareGraphicsRule{.eps}{eps}{.eps}{}
%
%  Include the listings package
%
\usepackage{listings}
%
%  Define Tiny Lisp based on Common Lisp
%
\lstdefinelanguage[Tiny]{Lisp}[]{Lisp}{morekeywords=[13]{atomp, bit-vector-p, car, cdr, char-downcase, char-code, char-upcase, compiled-function-p, dowhile, dump, exit, fresh-line, if, code-char, lambda, msg, nullp, parse-integer, peek8, peek16, peek32, poke8, poke16, poke32, progn, quote, read-line, reset, setq, simple-bit-vector-p, simple-string-p, simple-vector-p, string-downcase, string-upcase, memb, meml, memw, sim-step, go, reg-val, num-reg, halted}}
\lstset{language=Ada}
%
% Macro definitions
%
\newcommand{\operation}[1]{\textbf{\texttt{#1}}}
\newcommand{\package}[1]{\texttt{#1}}
\newcommand{\function}[1]{\texttt{#1}}
\newcommand{\constant}[1]{\emph{\texttt{#1}}}
\newcommand{\keyword}[1]{\texttt{#1}}
\newcommand{\datatype}[1]{\texttt{#1}}
\newcommand{\comment}[1]{{\color{red}{#1}}}
%
\newcommand{\docname}{Users's Manual for \\ CPU Simulators}

%
% Front Matter
%
\title{\docname}
\author{Brent Seidel \\ Phoenix, AZ}
\date{ \today }
%========================================================
%%% BEGIN DOCUMENT
\begin{document}
%
%  Header's and Footers
%
\fancypagestyle{plain}{
  \fancyhead[L]{}%
  \fancyhead[R]{}%
  \fancyfoot[C]{Page \thepage\ of \pageref{LastPage}}%
  \fancyfoot[L]{CPU Simulation}
  \renewcommand{\headrulewidth}{0pt}%
  \renewcommand{\footrulewidth}{0.4pt}%
}
\fancypagestyle{myfancy}{
  \fancyhead[L]{\docname}%
  \fancyhead[R]{\leftmark}
  \fancyfoot[C]{Page \thepage\ of \pageref{LastPage}}%
  \fancyfoot[L]{CPU Simulation}
  \renewcommand{\headrulewidth}{0.4pt}%
  \renewcommand{\footrulewidth}{0.4pt}%
}
\pagestyle{myfancy}
%
% Produce the front matter
%
\maketitle
\begin{center}
This document is \copyright 2026 Brent Seidel.  All rights reserved.

\paragraph{}Note that this is a draft version and not the final version for publication.
\end{center}
\tableofcontents

%========================================================
\chapter{Introduction}
This project includes simulators for some existing processors.
It can be embedded into other code as a library, or used stand-alone with a command line interpreter.
The intent of these simulators are to provide instruction level simulation and not hardware level.
Generally, no attempt has been made to count clock cycles - instructions may not even take the same relative amount of time to execute.

Interfaces are provided to allow the simulator to be controlled by and display data on a simulated control panel (see the Pi-Mainframe (\url{https://github.com/BrentSeidel/Pi-Mainframe}) project.
These may be stubbed out or ignored if not needed.

\section{Disclaimer}
I have tried to make the documentation and software as accurate as possible.
However, it is inevitable that errors exist.
Should you find an error, feel free to write an issue on the GitHub project and I will look at it.
For software errors especially, please provide information as to what change is needed.
If you would like some particular hardware added, feel free to request that as well.
Note that this is a hobby project maintained by one person who has other demands on their time.

\section{License}
This project is licensed using the GNU General Public License V3.0.
Should you wish other licensing terms, contact the author.

THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, EXPRESS OR IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY, FITNESS FOR A PARTICULAR PURPOSE AND NONINFRINGEMENT. IN NO EVENT SHALL THE AUTHORS BE LIABLE FOR ANY CLAIM, DAMAGES OR OTHER LIABILITY, WHETHER IN AN ACTION OF CONTRACT, TORT OR OTHERWISE, ARISING FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER DEALINGS IN THE SOFTWARE.

%========================================================
\chapter{How To Obtain}
This package is currently available on GitHub at \url{https://github.com/BrentSeidel/Sim-CPU}

\section{Dependencies}
\subsection{Ada Libraries}
The following Ada packages are used:
\begin{itemize}
  \item \package{Ada.Calendar}
  \item \package{Ada.Characters.Latin\_1}
  \item \package{Ada.Containers.Indefinite\_Ordered\_Maps}
  \item \package{Ada.Direct\_IO}
  \item \package{Ada.Exceptions}
  \item \package{Ada.Integer\_Text\_IO}
  \item \package{Ada.Streams}
  \item \package{Ada.Strings.Fixed}
  \item \package{Ada.Strings.Maps.Constants}
  \item \package{Ada.Strings.Unbounded}
  \item \package{Ada.Text\_IO}
  \item \package{Ada.Text\_IO.Unbounded\_IO}
  \item \package{Ada.Unchecked\_Conversion}
\end{itemize}


\subsection{Other Libraries}
This library depends on the root package \package{BBS} available at \url{https://github.com/BrentSeidel/BBS-Ada} or using alire via ``\keyword{alr get bbs}'' and the \package{BBS.Lisp} package available at \url{https://github.com/BrentSeidel/Ada-Lisp} or using alire via ``\keyword{alr get bbs\_lisp}''.

%========================================================
\chapter{Usage Instructions}

\section{Using Alire}
Alire automatically handles dependencies.
To use this in your project, just issue the command ``\keyword{alr with bbs\_simcpu}'' in your project directory.
To build the standalone CLI program, first obtain the cli using ``\keyword{alr get simcpucli}''.
Change to the appropriate directory and use ``\keyword{alr build}'' and ``\keyword{alr run}''.
To use the load CP/M program, use ``\keyword{alr get loadcpm}''.
Change to the appropriate directory and use ``\keyword{alr build}'' and ``\keyword{alr run}''.

\section{Using \keyword{gprbuild}}
This is a library of routines intended to be used by some program.
To use these in your program, edit your *\keyword{.gpr} file to include a line to \keyword{with} the path to \keyword{bbs\_simcpu\_noalr.gpr}.
Then in your Ada code \keyword{with} in the package(s) you need and use the routines.

This is also available as a standalone program with a command line interface (CLI).
This can be used for development or debugging.
To build the CLI, just build the \keyword{simcli\_noalr.gpr} project file, after making sure that the dependencies are in the expected places.

There is another program \keyword{loadcpm} available to create a bootable CP/M floppy disk image.
It is build using the \keyword{loadcpm\_noalr.gpr} project.
It takes an Intel hex file image and writes it to a floppy disk image.

%========================================================
\chapter{API Description}
\label{chap:API}
%--------------------------------------------------------------------------------------------------
\section{General}
Several simulators are available for use.
Each simulator may also have variation.
So, one simulator may provide variations for different processors in a family of processors.

Each simulator is based on an object that derives from the \datatype{simulator} object defined in the \package{BBS.Sim\_CPU} package.
A generic simulator interface is defined with some procedures or functions that must be defined by a specific simulator and some that may be defined, if needed.
There are also a number of utility functions that are not expected to be overridden by a specific simulator.

The current design has memory included in the simulator instead of being an external device.
Any I/O decoding is also handled inside the simulator.
This means that any reading and writing of memory has to be done via routines defined by the simulator.

%--------------------------------------------------------------------------------------------------
\section{Package Hierarchy}
The packages for the simulator are arranged in a hierarchy to functionally decompose the simulation.

\begin{itemize}
  \item \package{BBS.Sim\_CPU} - This is the root package for the simulator.
  \begin{itemize}
    \item \package{BBS.Sim\_CPU.bus} - This is the root package for different kinds of data busses.
    \begin{itemize}
      \item \package{BBS.Sim\_CPU.bus.mem8} - A simple 8 bit bus.  Contains objects for a bus both with and without separate I/O space.
      \item \package{BBS.Sim\_CPU.bus.pdp11} - A bus specific to the PDP-11 simulation.  It could be a Unibus or Qbus.
    \end{itemize}
    \item \package{BBS.Sim\_CPU.cpu} - This is the root package for the CPU simulations.
    \begin{itemize}
      \item \package{BBS.Sim\_CPU.cpu.i8080} - This contains simulations for the Intel 8080, 8085, and the Zilog Z80.  Further sub-packages are not listed.
      \item \package{BBS.Sim\_CPU.cpu.m68000} - This contains simulations for the 68000 family of processors.  Further sub-packages are not listed.
      \item \package{BBS.Sim\_CPU.cpu.msc6502} - This contains the simulation for the 6502 processor.
      \item \package{BBS.Sim\_CPU.cpu.pdp11} - This contains simulations for the PDP-11 family of processors.  Further sub-packages are not listed.
    \end{itemize}
    \item \package{BBS.Sim\_CPU.io} - This is the root package for all I/O devices.
    \begin{itemize}
      \item \package{BBS.Sim\_CPU.io.bm792} - This is a PDP-11 boot ROM containing code to boot a RK05 disk on a RK11 controller.
      \item \package{BBS.Sim\_CPU.io.clock} - This contains a generic clock device.
      \begin{itemize}
        \item \package{BBS.Sim\_CPU.io.clock.kw11} - This is a KW11 clock for PDP-11 computers
      \end{itemize}
      \item \package{BBS.Sim\_CPU.io.disk} - This contains definitions common for all disk type devices
      \begin{itemize}
        \item \package{BBS.Sim\_CPU.io.disk.floppy} - This contains generic floppy and hard disk controllers.
        \item \package{BBS.Sim\_CPU.io.disk.rk11} - This is a simulation of a RK11 controller for RK05 disks for PDP-11 computers.
        \item \package{BBS.Sim\_CPU.io.disk.rk611} - This is a simulation of a RK611 controller for RK07 disks (RK06 disks are not implemented) for PDP-11 computers.
        \item \package{BBS.Sim\_CPU.io.disk.tm11} - This is a simulation of a TM11 tape drive controller for PDP-11 computers.  It may eventually move to the ...tape package.
      \end{itemize}
      \item \package{BBS.Sim\_CPU.io.kt11} - This is a simulation of a KT11 memory management unit for PDP-11 computers.
      \item \package{BBS.Sim\_CPU.io.serial} - This contains definitions and a basic implementation for serial interfaces.
      \begin{itemize}
        \item \package{BBS.Sim\_CPU.io.serial.dl11} - This is a simulation of a DL11 single line interface for PDP-11 computers.
        \item \package{BBS.Sim\_CPU.io.serial.dz11} - This is a simulation of a DZ 8 line terminal multiplexer for PDP-11 computers.
        \item \package{BBS.Sim\_CPU.io.serial.mux} - This is a basic 8 line terminal multiplexer.
        \item \package{BBS.Sim\_CPU.io.serial.telnet} - This is a basic single line interface.
      \end{itemize}
      \item \package{BBS.Sim\_CPU.io.tape} - Contains definitions and an implementation of a basic paper tape device.
      \begin{itemize}
        \item \package{BBS.Sim\_CPU.io.tape.pc11} - This is a simulation of a PC11 paper tape reader/punch for PDP-11 computers.
      \end{itemize}
    \end{itemize}
  \end{itemize}
\end{itemize}

%--------------------------------------------------------------------------------------------------
\section{Numerical Data Types}
Currently, processors with address and data busses up to 32 bits wide are supported.
For the address bus, use the data type \datatype{addr\_bus}  and for the data bus, use \datatype{data\_bus}.
These are both defined as a 32 bit unsigned integer.
Each simulator may use as many or as few of these bits as are needed.
This means that if (as in most cases) a simulator doesn't define a full 4 GB of memory, the external program can try to read or write non-existent memory.
The bus object should check for this, otherwise this will probably cause an exception.

%--------------------------------------------------------------------------------------------------
\section{Enumeration Data Types}
The \datatype{proc\_mode} enumeration is used to indicate the processor mode.
Not all modes will be used be any given processor and the meanings are processor specific.
Any of these modes could be used by a processor that doesn't define multiple modes.
The defined modes are:
\begin{itemize}
  \item \constant{PROC\_NONE} - Unspecified processor mode.
  \item \constant{PROC\_KERN} - Kernel mode.  This is usually the most privileged mode.
  \item \constant{PROC\_EXEC} - Executive mode.  This is a little less privileged.
  \item \constant{PROC\_SUP} - Supervisor mode.  This is a little less privileged.
  \item \constant{PROC\_USER} - User mode.  This is usually the least privileged mode.
\end{itemize}

The \datatype{addr\_type} enumeration is used to indicate the type of memory access.
They may be used for controlling access to memory.
The defined types are:
\begin{itemize}
  \item \constant{ADDR\_NONE} - Unspecified address type.
  \item \constant{ADDR\_INTR} - Reading interrupt data, typically an interrupt vector.
  \item \constant{ADDR\_DATA} - Reading or writing data.
  \item \constant{ADDR\_INS} - Reading instructions (probably not writing).
  \item \constant{ADDR\_IO} - Reading or writing to I/O ports for processors that have separate I/O addresses.
\end{itemize}

The \datatype{bus\_type} enumeration defines the type of bus for attaching I/O devices and bus accesses.
This can either be the I/O bus (for processors with separate I/O space) or the memory bus for memory and memory mapped I/O.
The defined busses are:
\begin{itemize}
  \item \constant{BUS\_MEMORY} - Use the memory bus.
  \item \constant{BUS\_IO} - Use the I/O bus
\end{itemize}

The \datatype{bus\_size} enumeration defines the desired size of a bus transaction.
The defined sizes are:
\begin{itemize}
  \item \constant{bits0} - Zero bit (null) transaction.  Not currently used.
  \item \constant{bits8} - Eight bit (byte) transaction.
  \item \constant{bits16} - Sixteen bit (word) transaction.
  \item \constant{bits32} - Thirty-two bit (long-word) transaction.  Not currently used.
\end{itemize}

The \datatype{bus\_stat} enumeration is used for reporting the result of a bus transaction.
The defined results are:
\begin{itemize}
  \item \constant{BUS\_SUCC} - The transaction succeeded.  This is the result that you hope you get.
  \item \constant{BUS\_NONE} - Nothing exists at this address.  Either uninstalled memory or non-existent I/O device.
  \item \constant{BUS\_ALIGN} - Illegal alignment, such as an odd address
  \item \constant{BUS\_PROT} - A protection violation occurred.  This is typically reported by a memory management unit when the \datatype{proc\_mode} doesn't match the required level for the address.
  \item \constant{BUS\_PARITY} - A parity error occurred during the bus transaction.  This would usually be used for testing error handlers.
  \item \constant{BUS\_MMU} - Error was reported by memory management unit
  \item \constant{BUS\_ECC1} - A correctable ECC error occurred during the bus transaction.  This would usually be used for testing error handlers.
  \item \constant{BUS\_ECC2} - An uncorrectable ECC error occurred during the bus transaction.  This would usually be used for testing error handlers
\end{itemize}

%========================================================
\chapter{The CPU Objects}
\label{chap:CPU}
The CPU object is responsible for executing instructions.
Generally before using a simulator, it must be initialized, a variant selected, and a bus attached.
However this is all implementation dependent.

%--------------------------------------------------------------------------------------------------
\section{Tracing}
Depending of flag settings, information can be displayed during simulator processing.
The API presents this as a \datatype{trace\_flags} record value.
Two \package{Unchecked\_Conversion} functions are provided for converting between this and \datatype{uint32}.
These routines are \function{uint32\_to\_trace} and \function{trace\_to\_uint32}.
Currently only the 6 LSBs are used, the rest are reserved for future use.
\begin{itemize}
  \item 0   List instructions being traced.
  \item 1   List I/O operations.
  \item 2   List data operations.
  \item 3   List bus specific items.
  \item 4   List control transfers.
  \item 5   List exceptions.
\end{itemize}

Set the trace flags.
\begin{lstlisting}
procedure trace(self : in out simulator; l : trace_flags);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
  \item \keyword{l} - The trace flags
\end{itemize}

%--------------------------------------------------------------------------------------------------
\section{Primary API}
To initialize the CPU, use:
\begin{lstlisting}
procedure init(self : in out simulator) is abstract;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object to initialize.
\end{itemize}

To select a CPU variant, use:
\begin{lstlisting}
procedure variant(self : in out simulator; v : Natural) is abstract;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object to initialize.
  \item \keyword{v} - The index of the variant to use.  This value is defined by the specific CPU object.
\end{itemize}

Attaching a bus to a CPU also requires attaching the cpu to the bus.
This gives both the CPU and the bus object a reference to each other.
The routine to attach a bus to a CPU automatically calls the routine to attach the CPU the bus.
To attach a bus, use (note that this is declared as null since some CPUs may not need this and thus they won't have to create a null implementation):
\begin{lstlisting}
procedure attach_bus(self : in out simulator;
                     bus : BBS.Sim_CPU.bus.bus_access;
                     index : Natural) is Null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object to initialize.
  \item \keyword{bus} - The bus object to attach.
  \item \keyword{index} - The CPU index.  Expected to be used for future multi-CPU simulations.  Currently unused by all defined bus objects.
\end{itemize}

Called once when Start/Stop switch is moved to start position.
This typically sets the program counter, but may do other CPU specific things.
\begin{lstlisting}
procedure start(self : in out simulator) is Null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
\end{itemize}

Called to start simulator execution at a specific address.
This is made null rather than abstract so that simulators that don't use it don't need to override it.
\begin{lstlisting}
procedure start(self : in out simulator; addr : addr_bus) is null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
\end{itemize}

Called once per frame to execute the next instruction.
\begin{lstlisting}
procedure run(self : in out simulator) is Null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
\end{itemize}

%--------------------------------------------------------------------------------------------------
\section{Other API}
Some of these routines are only used in specific cases, such as when interfaced to switches and light.
Others have more general application.

Clear a breakpoint at a specific address.
\begin{lstlisting}
   procedure clearBreak(self : in out simulator; addr : addr_bus) is null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
  \item \keyword{addr} - The address.
\end{itemize}

\subsection{Interrupt Related Routines}
Cancel an interrupt if not already taken.
\begin{lstlisting}
procedure cancelInterrupt(self : in out simulator; data : long) is null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
  \item \keyword{data} - The interrupt code.  Interpreted by the simulator.
\end{itemize}

\subsection{Halt/Running Related Routines}
Clear the halted flag to allowing processing to continue.
\begin{lstlisting}
procedure continue_proc(self : in out simulator) is null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
\end{itemize}

Force a simulator to enter a halt state (if implemented).
Can be used for some error conditions where the CPU needs to be halted.
\begin{lstlisting}
procedure halt(self : in out simulator) is null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
\end{itemize}

Check if simulator is halted.
\begin{lstlisting}
function halted(self : in out simulator) return Boolean is (False);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
\end{itemize}

Request a processor reset.
\begin{lstlisting}
   procedure reset(self : in out simulator) is null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
\end{itemize}

Sets a breakpoint at a specific address.
\begin{lstlisting}
   procedure setBreak(self : in out simulator; addr : addr_bus) is null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
  \item \keyword{addr} - The address.
\end{itemize}

Return the current value of the trace flags.
\begin{lstlisting}
function trace(self : in out simulator) return trace_flags;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
\end{itemize}

\subsection{Interrupt Related Routines}
Signal an interrupt from a device.
\begin{lstlisting}
procedure interrupt(self : in out simulator; data : long) is null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
  \item \keyword{data} - The interrupt code.  Interpreted by the simulator.
\end{itemize}

Allow an external user to enable or disable processing of interrupts
\begin{lstlisting}
procedure interrupts(self : in out simulator; state : Boolean) is null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
  \item \keyword{state} - \constant{True} for enable, \constant{False} for disable.
\end{itemize}

Returns simulator dependent status of interrupts as \datatype{int32}.
This may be something like enabled/disabled or processor priority.
\begin{lstlisting}
   function intStatus(self : in out simulator) return int32 is (0);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
\end{itemize}

\subsection{Memory Related Routines}

Note that memory could be accessed more directly via the bus object.

Load data into the simulator.
The simulator is responsible for interpreting \keyword{name}, decoding, and loading the data.
\begin{lstlisting}
procedure load(self : in out simulator; name : String) is null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
  \item \keyword{name} - Typically used as a file name.
\end{itemize}
\subsection{Memory Related Routines}

Read a value from an address in simulator memory.
If simulator has memory mapped I/O, this could also be an I/O device.
\begin{lstlisting}
function read_mem(self : in out simulator; mem_addr : addr_bus)
                  return data_bus is (0);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
  \item \keyword{mem\_addr} - The memory address to access.
\end{itemize}

Write a value to an address in simulator memory.
If simulator has memory mapped I/O, this could also be an I/O device.
\begin{lstlisting}
procedure set_mem(self : in out simulator; mem_addr : addr_bus;
                  data : data_bus) is Null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
  \item \keyword{mem\_addr} - The memory address to access.
  \item \keyword{data} - The value to write to the memory address.
\end{itemize}

\subsection{Simulator Data Related Routines}
Returns the simulator name as a string.
\begin{lstlisting}
function name(self : in out simulator) return String is ("No simulator");
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
\end{itemize}

Return a simulator option value as a string.  This is highly simulator specific.
\begin{lstlisting}
function option(self : in out simulator;
                opt : String) return String is ("");
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
  \item \keyword{opt} - The name of the option.
\end{itemize}

Set a simulator specific option.  This is highly simulator specific.
\begin{lstlisting}
procedure option(self : in out simulator;
                 opt : String; value : String) is null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
  \item \keyword{opt} - The name of the option.
  \item \keyword{value} - The new value for the option.
\end{itemize}

Returns the variant name as a string.
\begin{lstlisting}
function variant(self : in out simulator;
                 v : Natural) return String is ("No variant");
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
\end{itemize}

Returns the current variant index as a \datatype{Natural}.
\begin{lstlisting}
function variant(self : in out simulator) return Natural is (0);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
\end{itemize}

Set the variant to the specified value.
\begin{lstlisting}
procedure variant(self : in out simulator; v : Natural) is Null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
  \item \keyword{v} - The variant index.
\end{itemize}

Returns the number of variants as a \datatype{Natural}.
\begin{lstlisting}
function variants(self : in out simulator) return Natural is (1);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
\end{itemize}

\subsection{Register Related Routines}

Return the value of the specified register as \datatype{uint32}.
\begin{lstlisting}
function read_reg(self : in out simulator; num : uint32)
                  return data_bus is (0);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
  \item \keyword{num} - The register.
\end{itemize}

Return the value of the specified register as a \datatype{String}.
This would normally be used, for example, to return a formatted representation of the condition codes or PSW.
\begin{lstlisting}
function read_reg(self : in out simulator; num : uint32)
                  return String is ("No Reg");
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
  \item \keyword{num} - The register.
\end{itemize}
Returns the number of simulator registers as a \datatype{uint32}.
What constitutes a register is simulator dependent.
\begin{lstlisting}
function registers(self : in out simulator) return uint32 is (0);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
\end{itemize}

Returns a \datatype{String} representing the name of the specified register.
\begin{lstlisting}
function reg_name(self : in out simulator; num : uint32)
                  return String is ("No Reg");
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
  \item \keyword{num} - The register.
\end{itemize}

Sets the specified register to the specified value.
\begin{lstlisting}
procedure set_reg(self : in out simulator; num : uint32;
                  data : data_bus) is Null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
  \item \keyword{num} - The register.
  \item \keyword{data} - The value for the register.
\end{itemize}

%--------------------------------------------------------------------------------------------------
\section{Special Purpose API}
While the routines in this section are publicly available, they are intended for specific applications.

Called once when the Deposit switch is moved to the Deposit position.  Used for blinkenlights.
\begin{lstlisting}
procedure deposit(self : in out simulator) is Null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
\end{itemize}

Called once when the Examine switch is moved to the Examine position.  Used for blinkenlights.
\begin{lstlisting}
procedure examine(self : in out simulator) is Null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
\end{itemize}

Return the last address used when writing on the I/O bus.
Obviously, this only applies to CPUs that have separate I/O space.
\begin{lstlisting}
function lastOutAddr(self : in out simulator) return addr_bus is (0);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
\end{itemize}

Return the last data written when writing on the I/O bus.
Obviously, this only applies to CPUs that have separate I/O space.
\begin{lstlisting}
function lastOutData(self : in out simulator) return data_bus is (0);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
\end{itemize}

Override an I/O read operation at a specified address with specified data.
Obviously, this only applies to CPUs that have separate I/O space.
\begin{lstlisting}
procedure overrideIn(self : in out simulator; addr : in addr_bus;
                     data : in data_bus) is null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
  \item \keyword{addr} - The address to override.
  \item \keyword{data} - The value to provide for a read to the specified address.
\end{itemize}

%========================================================
\chapter{CPU Variants Currently Supported}
\label{chap:CPUVar}
%--------------------------------------------------------------------------------------------------
\section{Example}
The example simulator provides an example of using the simulator object interface.
Its primary purpose is to blink the lights in interesting ways in the Pi-Mainframe (\url{https://github.com/BrentSeidel/Pi-Mainframe}) project.
There are a number of different patterns selectable.
Variants are defined for ``Copy Switches'', ``Count'', ``16-Bit Scan'', ``16-Bit Bouncer'', ``Fibonacci Counter'', ``32-Bit Scan'', and ``32-Bit Bouncer''.

This simulator is unusual in that it has no memory defined, but instead has several registers defined that act as memory.
When reading or writing memory, the address is ignored and the value returned depends on the pattern (variant) selected.

%--------------------------------------------------------------------------------------------------
\section{8080 Family}
The 8080 simulator has variants defined for the 8080, 8085, and Z-80 processors.
These are 8 bit processors with an 8 bit data bus and a 16 bit address bus.
In addition to a memory bus, these processors also include an I/O bus with 8 bit I/O port addressing.
See \cite{i8080} for more information about the 8080/8085.
See \cite{z80} and \cite{z80undoc} for more information about the Z-80.

Currently, the 8080 family does not have memory mapped I/O.  This may be added at some time in the future.

When loading data using the \function{load} routine, the specified file is assumed to be in Intel Hex format.

%--------------------------------------------------------------------------------------------------
\section{68000 Family}
The 68000 simulator has variants defined for the 68000, 68008, 68010, and CPU32.
Only the 68000 and 68008 are currently implemented.
The 68010 and CPU32 are for future development.
Internally, these are 32 bit processors with 32 bit data and 32 bit address busses.
These processors do not have a separate I/O address space.
All I/O is mapped as part of the main memory.
The external address and data bus sizes depend on the variant selected.
See \cite{m68000a} and \cite{m68000b} for more information about the 68000 family.

The interrupt code is interpreted with the low order bits (7-0) representing the vector number and the next 8 bits (15-8) representing the priority.
Interrupts with vectors that match internally defined exception vectors are ignored.
Thus only interrupt vectors in the range 25-31 and 64-255 are processed.

When loading data using the \function{load} routine, the specified file is assumed to be in Motorola S-Record format.

%--------------------------------------------------------------------------------------------------
\section{6502 Family}
The 6502 simulator currently has only a working variants defined for the 6502 processor.
This is an 8 bit processors with an 8 bit data bus and a 16 bit address bus.
This processor does not have a separate I/O address space.
All I/O is mapped as part of the main memory.
Three interrupts are define for normal interrupts, non-maskable interrupts, and reset.
See \cite{msc6502} for more information about the 6502.

When loading data using the \function{load} routine, the specified file is assumed to be in Intel Hex format.

%--------------------------------------------------------------------------------------------------
\section{PDP-11 Family}
Most of the 16 bit addressing members are supported, except the LSI-11, PDP-11/03.
Future models with memory management are planned at some point.
This is a line of 16 bit minicomputers that was developed in the 1970s and was popular through the 1990s.
Models with no memory management had 16 address bit.
With memory management, depending on the model, addresses of 18 or 22 bits was supported.
These processors do not have a separate I/O address space.
All I/O is mapped as part of the main memory.
By convention, all I/O is located in the upper 8kBytes of memory.
See \cite{dec80} and \cite{dec84} for more information about the PDP-11 in general.

A wealth of information about the PDP-11 (and other DEC hardware) is on bitsavers at \url{https://www.bitsavers.org/pdf/dec/}.
Software can also be found on bitsavers at \url{https://www.bitsavers.org/bits/DEC/}.
Various diagnostic software can be found using the PDP-11 Diagnostics - Database at \url{https://www.retrocmp.com/tools/pdp-11-diagnostic-database/202-pdp-11-diagnostics-database}.

%========================================================
\chapter{The Bus Objects}
\label{chap:Bus}
This bus object contains the memory for the CPU and interfaces with the I/O devices.
Itt provides routines for accessing memory by physical or logical addresses.
In some cases these will be identical.
When some sort of address translation is provided, such as bank switching or a full MMU, they may be different.
All memory access routines provide a \keyword{status} parameter that contains the \datatype{bus\_state} the status of the operation.
If \keyword{status} is not \constant{BUS\_SUCC}, the operation did not complete and any data returned by a read operation should be ignored.

%--------------------------------------------------------------------------------------------------
\section{Special API}
Once the bus object is created, the main action a host program needs to do is to attach I/O devices.
While this is exposed publicly, it's main purpose is to be used by a CPU object when a bus is attached to the
CPU.
In most, if not all cases, it should not be used by user code.
\begin{lstlisting}
procedure attach_io(self : in out bus;
                    io_dev : BBS.Sim_CPU.io.io_access;
                    base_addr : addr_bus;
                    which_bus : bus_type) is abstract;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to initialize.
  \item \keyword{io\_dev} - The I/O device object to attach to the bus.
  \item \keyword{base\_addr} - The base address to use to access the device registers.
  \item \keyword{which\_bus} - One of the defined \datatype{bus\_type} values.
\end{itemize}

Called to attach an I/O device to a bus at a specific address.
Bus is simulator dependent as some CPUs have separate I/O and memory space.
\begin{lstlisting}
procedure attach_io(self : in out bus; io_dev : BBS.Sim_CPU.io.io_access;
                    base_addr : addr_bus; which_bus : bus_type) is Null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
  \item \keyword{io\_dev} - Access to the I/O object being attached.
  \item \keyword{base\_addr} - The base address for the device registers.
  \item \keyword{which\_bus} - Indicates if the device should be attached to memory or I/O (if available) bus.
\end{itemize}

Called once when the Deposit switch is moved to the Deposit position.  Used for blinkenlights.
\begin{lstlisting}
procedure deposit(self : in out simulator) is Null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
\end{itemize}

Called once when the Examine switch is moved to the Examine position.  Used for blinkenlights.
\begin{lstlisting}
procedure examine(self : in out simulator) is Null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The simulator object.
\end{itemize}

%--------------------------------------------------------------------------------------------------
\section{Memory Access API}

To read from physical memory, use:
\begin{lstlisting}
function readp(self : in out bus; addr : addr_bus; status : out bus_stat)
               return data_bus is abstract;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
  \item \keyword{addr} - The memory address to read.
  \item \keyword{status} - the \datatype{bus\_status} result of the operation.
  \item Returns the value at the specified address.
\end{itemize}

To write to physical memory, use:
\begin{lstlisting}
procedure writep(self : in out bus; addr : addr_bus; data: data_bus;
                 status : out bus_stat) is abstract;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
  \item \keyword{addr} - The memory address to read.
  \item \keyword{data} - The value to write to memory.
  \item \keyword{status} - the \datatype{bus\_status} result of the operation.
\end{itemize}

To read from logical memory, use:
\begin{lstlisting}
function readl(self : in out bus; addr : addr_bus; mode : proc_mode;
               addr_kind : addr_type; status : out bus_stat)
               return data_bus is abstract;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
  \item \keyword{addr} - The memory address to read.
  \item \keyword{mode} - The \datatype{proc\_mode} for the operation.
  \item \keyword{addr\_kind} - The \datatype{addr\_type} for the operation.
  \item \keyword{status} - the \datatype{bus\_status} result of the operation.
  \item Returns the value at the specified address.
\end{itemize}

To write to logical memory, use:
\begin{lstlisting}
procedure writel(self : in out bus; addr : addr_bus; data: data_bus;
                 mode : proc_mode; addr_kind : addr_type;
                 status : out bus_stat) is abstract;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
  \item \keyword{addr} - The memory address to read.
  \item \keyword{data} - The value to write to memory.
  \item \keyword{mode} - The \datatype{proc\_mode} for the operation.
  \item \keyword{addr\_kind} - The \datatype{addr\_type} for the operation.
  \item \keyword{status} - the \datatype{bus\_status} result of the operation.
\end{itemize}

For reading from logical memory in bytes, words, and long words in LSB first or MSB first format, the following are provided:

LSB First:
\begin{lstlisting}
function readl8l(self : in out bus; addr : addr_bus; mode : proc_mode;
               addr_kind : addr_type; status : out bus_stat)
               return byte is abstract;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
  \item \keyword{addr} - The memory address to read.
  \item \keyword{mode} - The \datatype{proc\_mode} for the operation.
  \item \keyword{addr\_kind} - The \datatype{addr\_type} for the operation.
  \item \keyword{status} - the \datatype{bus\_status} result of the operation.
  \item Returns the \datatype{byte} at the specified address.
\end{itemize}

\begin{lstlisting}
function readl16l(self : in out bus; addr : addr_bus; mode : proc_mode;
               addr_kind : addr_type; status : out bus_stat)
               return word is abstract;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
  \item \keyword{addr} - The memory address to read.
  \item \keyword{mode} - The \datatype{proc\_mode} for the operation.
  \item \keyword{addr\_kind} - The \datatype{addr\_type} for the operation.
  \item \keyword{status} - the \datatype{bus\_status} result of the operation.
  \item Returns the \datatype{word} at the specified address with LSB first.
\end{itemize}

\begin{lstlisting}
function readl32l(self : in out bus; addr : addr_bus; mode : proc_mode;
               addr_kind : addr_type; status : out bus_stat)
               return long is abstract;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
  \item \keyword{addr} - The memory address to read.
  \item \keyword{mode} - The \datatype{proc\_mode} for the operation.
  \item \keyword{addr\_kind} - The \datatype{addr\_type} for the operation.
  \item \keyword{status} - the \datatype{bus\_status} result of the operation.
  \item Returns the \datatype{long} at the specified address with LSB first.
\end{itemize}

MSB First:
\begin{lstlisting}
function readl8m(self : in out bus; addr : addr_bus; mode : proc_mode;
               addr_kind : addr_type; status : out bus_stat)
               return byte is abstract;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
  \item \keyword{addr} - The memory address to read.
  \item \keyword{mode} - The \datatype{proc\_mode} for the operation.
  \item \keyword{addr\_kind} - The \datatype{addr\_type} for the operation.
  \item \keyword{status} - the \datatype{bus\_status} result of the operation.
  \item Returns the \datatype{byte} at the specified address (this is the same as readl8l).
\end{itemize}

\begin{lstlisting}
function readl16m(self : in out bus; addr : addr_bus; mode : proc_mode;
               addr_kind : addr_type; status : out bus_stat)
               return word is abstract;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
  \item \keyword{addr} - The memory address to read.
  \item \keyword{mode} - The \datatype{proc\_mode} for the operation.
  \item \keyword{addr\_kind} - The \datatype{addr\_type} for the operation.
  \item \keyword{status} - the \datatype{bus\_status} result of the operation.
  \item Returns the \datatype{word} at the specified address with MSB first.
\end{itemize}

\begin{lstlisting}
function readl32m(self : in out bus; addr : addr_bus; mode : proc_mode;
               addr_kind : addr_type; status : out bus_stat)
               return long is abstract;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
  \item \keyword{addr} - The memory address to read.
  \item \keyword{mode} - The \datatype{proc\_mode} for the operation.
  \item \keyword{addr\_kind} - The \datatype{addr\_type} for the operation.
  \item \keyword{status} - the \datatype{bus\_status} result of the operation.
  \item Returns the \datatype{long} at the specified address with MSB first.
\end{itemize}

For writing to logical memory in bytes, words, and long words in LSB first or MSB first format, the following are provided:

LSB first:
\begin{lstlisting}
procedure writel8l(self : in out bus; addr : addr_bus;
                 data: byte; mode : proc_mode;
                 addr_kind : addr_type; status : out bus_stat) is abstract;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
  \item \keyword{addr} - The memory address to read.
  \item \keyword{data} - The \datatype{byte} to write to memory.
  \item \keyword{mode} - The \datatype{proc\_mode} for the operation.
  \item \keyword{addr\_kind} - The \datatype{addr\_type} for the operation.
  \item \keyword{status} - the \datatype{bus\_status} result of the operation.
\end{itemize}

\begin{lstlisting}
procedure writel16l(self : in out bus; addr : addr_bus;
                 data: byte; mode : proc_mode;
                 addr_kind : addr_type; status : out bus_stat) is abstract;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
  \item \keyword{addr} - The memory address to read.
  \item \keyword{data} - The \datatype{word} to write to memory (LSB first).
  \item \keyword{mode} - The \datatype{proc\_mode} for the operation.
  \item \keyword{addr\_kind} - The \datatype{addr\_type} for the operation.
  \item \keyword{status} - the \datatype{bus\_status} result of the operation.
\end{itemize}

\begin{lstlisting}
procedure writel32l(self : in out bus; addr : addr_bus;
                 data: byte; mode : proc_mode;
                 addr_kind : addr_type; status : out bus_stat) is abstract;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
  \item \keyword{addr} - The memory address to read.
  \item \keyword{data} - The \datatype{long} to write to memory (LSB first).
  \item \keyword{mode} - The \datatype{proc\_mode} for the operation.
  \item \keyword{addr\_kind} - The \datatype{addr\_type} for the operation.
  \item \keyword{status} - the \datatype{bus\_status} result of the operation.
\end{itemize}

MSB first:
\begin{lstlisting}
procedure writel8m(self : in out bus; addr : addr_bus;
                 data: byte; mode : proc_mode;
                 addr_kind : addr_type; status : out bus_stat) is abstract;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
  \item \keyword{addr} - The memory address to read.
  \item \keyword{data} - The \datatype{byte} to write to memory.
  \item \keyword{mode} - The \datatype{proc\_mode} for the operation.
  \item \keyword{addr\_kind} - The \datatype{addr\_type} for the operation.
  \item \keyword{status} - the \datatype{bus\_status} result of the operation.
\end{itemize}

\begin{lstlisting}
procedure writel16m(self : in out bus; addr : addr_bus;
                 data: byte; mode : proc_mode;
                 addr_kind : addr_type; status : out bus_stat) is abstract;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
  \item \keyword{addr} - The memory address to read.
  \item \keyword{data} - The \datatype{word} to write to memory (MSB first).
  \item \keyword{mode} - The \datatype{proc\_mode} for the operation.
  \item \keyword{addr\_kind} - The \datatype{addr\_type} for the operation.
  \item \keyword{status} - the \datatype{bus\_status} result of the operation.
\end{itemize}

\begin{lstlisting}
procedure writel32m(self : in out bus; addr : addr_bus;
                 data: byte; mode : proc_mode;
                 addr_kind : addr_type; status : out bus_stat) is abstract;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
  \item \keyword{addr} - The memory address to read.
  \item \keyword{data} - The \datatype{long} to write to memory (MSB first).
  \item \keyword{mode} - The \datatype{proc\_mode} for the operation.
  \item \keyword{addr\_kind} - The \datatype{addr\_type} for the operation.
  \item \keyword{status} - the \datatype{bus\_status} result of the operation.
\end{itemize}

%--------------------------------------------------------------------------------------------------
\section{Bus Information API}

Return bus size as a \datatype{bus\_size} value.
\begin{lstlisting}
function size(self : in out bus) return bus_size is (bits0);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
\end{itemize}

The size that memory has been configured for when the bus object was created.
This should not change over the lifetime of the object.
\begin{lstlisting}
function mem_size(self : in out bus) return addr_bus is (0);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
\end{itemize}

For debugging (or maybe other) purposes, the maximum address can be set.
If greater than the configured size, this is ignored.
Accessing memory beyond the maximum address will return a \constant{BUS\_NONE} status.
\begin{lstlisting}
procedure set_max_addr(self : in out bus; size : addr_bus) is null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
  \item \keyword{size} - The new memory size.
\end{itemize}

Return this maximum address.
This should always be less than or equal to the configured size.
\begin{lstlisting}
function get_max_addr(self : in out bus) return addr_bus is (0);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
\end{itemize}

Send a reset signal to devices on the bus, if the bus supports it.
If not, nothing happens.
\begin{lstlisting}
procedure reset(self : in out bus) is null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
\end{itemize}

%--------------------------------------------------------------------------------------------------
\section{Blinkenlights API}
These are intended to be used with a blinkenlights project.
The routines here are intended to read switch values and drive LEDs.

Return the LED register data bus value.
\begin{lstlisting}
function get_lr_data(self : in out bus) return data_bus;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
\end{itemize}

Return the LED register address bus value.
\begin{lstlisting}
function get_lr_addr(self : in out bus) return addr_bus;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
\end{itemize}

Return the LED register control indication value
\begin{lstlisting}
function get_lr_ctrl(self : in out bus) return ctrl_mode;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
\end{itemize}

Return the address/data switch register value.
\begin{lstlisting}
function get_sr_ad(self : in out bus) return ad_bus;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
\end{itemize}

Return the control switch register value.
\begin{lstlisting}
function get_sr_ctrl(self : in out bus) return ctrl_mode;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
\end{itemize}

Set the LED register data bus value.
\begin{lstlisting}
procedure set_lr_data(self : in out bus; value : data_bus);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
  \item \keyword{value} - The value to set.
\end{itemize}

Set the LED register address bus value.
\begin{lstlisting}
procedure set_lr_addr(self : in out bus; value : addr_bus);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
  \item \keyword{value} - The value to set.
\end{itemize}

Set the LED register control indication value.
\begin{lstlisting}
procedure set_lr_ctrl(self : in out bus; value : ctrl_mode);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
  \item \keyword{value} - The value to set.
\end{itemize}

Set the switch register address/data value.
\begin{lstlisting}
procedure set_sr_ad(self : in out bus; value : ad_bus);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
  \item \keyword{value} - The value to set.
\end{itemize}

Set the switch register control switch value.
\begin{lstlisting}
procedure set_sr_ctrl(self : in out bus; value : ctrl_mode);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The bus object to access.
  \item \keyword{value} - The value to set.
\end{itemize}

%========================================================
\chapter{The I/O Objects}
\label{chap:IO}
I/O objects are used to simulate various I/O devices.
This chapter describes the API common to all I/O objects.
From the viewpoint of a CPU, an I/O device consists of a contiguous range of addresses for the device registers on either the memory or I/O bus.
These device registers are used to interface with and control the device.
Once a device has been attached to a bus, the CPU may communicate with it via the device registers.

If a device uses interrupts, the owning CPU must be set along with an exception code.
If a device uses DMA, the owning CPU must be specified (note that this may change to be the bus that the device is attached to).

Return a brief text description of the device as a \datatype{String}.
\begin{lstlisting}
function description(self : in out io_device)
         return String is ("Unimplemented");
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The I/O object to access.
\end{itemize}

Return the type/class of the device as a \datatype{dev\_type}.
\begin{lstlisting}
function dev_class(self : in out io_device)
         return dev_type is (NL);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The I/O object to access.
\end{itemize}

Get base address of device registers.
\begin{lstlisting}
function getBase(self : in out io_device) return addr_bus;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The I/O object to access.
\end{itemize}

Get size in bytes of number of addresses used for device registers.
\begin{lstlisting}
function getSize(self : in out io_device) return addr_bus is (0);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The I/O object to access.
\end{itemize}

Return text for the device name as a \datatype{String}.
\begin{lstlisting}
function name(self : in out io_device)
         return String is ("Unimplemented");
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The I/O object to access.
\end{itemize}

To read from the device registers use:
\begin{lstlisting}
function read(self : in out io_device; addr : addr_bus)
         return data_bus is abstract;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The I/O object to access.
  \item \keyword{addr} - The address of the device register to access.
  \item Returns the value from the specified device register.
\end{itemize}

Send a reset signal to the device.
What the device does is device specific.
If this is not overridden by the device, nothing is done.
\begin{lstlisting}
procedure reset(self : in out io_device) is null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The I/O object to access.
\end{itemize}

Set the base address of the device registers.
Note that the device registers are assumed to occupy a contiguous block of addresses.
\begin{lstlisting}
procedure setBase(self : in out io_device; base : addr_bus);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The I/O object to access.
  \item \keyword{base} - The base address.
\end{itemize}

To set the exception code, use:
\begin{lstlisting}
procedure setException(self : in out io_device;
                       except : long) is abstract;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The I/O object to access.
  \item \keyword{except} - The exception code to use.  The interpretation of this code depends on the specific CPU.
\end{itemize}

To set the owning CPU, use:
\begin{lstlisting}
procedure setOwner(self : in out io_device;
                   owner : BBS.Sim_CPU.CPU.sim_access);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The I/O object to access.
  \item \keyword{owner} - The CPU object for the owning CPU.
\end{itemize}

Send a shutdown signal to the device.
What the device does is device specific.
If this is not overridden by the device, nothing is done.
\begin{lstlisting}
procedure shutdown(self : in out io_device) is null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The I/O object to access.
\end{itemize}

To write to the device registers use:
\begin{lstlisting}
procedure write(self : in out io_device; addr : addr_bus;
                data : data_bus) is abstract;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The I/O object to access.
  \item \keyword{addr} - The address of the device register to access.
  \item \keyword{data} - The data to write to the device register.
\end{itemize}

%========================================================
\chapter{I/O Devices}
\label{chap:IODev}
Each I/O device is based on an object that derives from the \datatype{io\_device} object defined in the \package{BBS.Sim\_CPU.io} package.
The basic API for all I/O devices is described in Chapter \ref{chap:IO}.
This chapter describes additional APIs for specific types of devices.

%--------------------------------------------------------------------------------------------------
\section{Device Names}
Each device has a name that can be used to refer to it.
The name consists of an alphabetic device code followed by a decimal unit number.
If no unit number is specified, 0 is assumed.
A unit number of zero is special.
It is used to refer to a generic device, such as when attaching a device - the unit number is assigned during the attachment.
The currently defined device codes are (note that not all are implemented in the CLI, but will eventually be):
\begin{description}
  \item[CLK] -- A clock device to generate a periodic interrupt.
  \item[CON] -- A console serial device.  In most cases, this should not be used.
  \item[FD] -- Main disk controller with a track and sector interface.  It started out as a floppy disk controller, but geometries can be defined beyond that.
  \item[HD] -- An experimental disk controller using just block numbers.  It is not yet implemented and tested, and may be abandoned.
  \item[MUX] -- An 8 channel terminal multiplexer using a telnet interface.
  \item[PRN] -- A simple printer output device.
  \item[PTP] -- A paper tape interface.  It can read and write simulated tapes.
  \item[TEL] -- A single channel terminal device using a telnet interface.
\end{description}
The following devices simulate actual hardware for PDP-11 computers.
\begin{description}
  \item[DL11] --  A single line serial interface, see \cite{DL11}.
  \item[DZ11] -- An 8 line serial interface multiplexer, see \cite{DZ11}.
  \item[KW11] -- A clock for the PDP-11 series of computer, see \cite{KW11L}.
  \item[RK11] -- A disk controller that can have up to 8 attached RK05 simulated drives, see \cite{RK11}.
  \item[RK611] -- A disk controller than can have up to 8 attached RK07 simulated drives (RK06 drives are not supported), see \cite{RK611}.
  \item[PC11] -- A combination paper tape reader/punch, see \cite{PC11}.
  \item[TM11] -- A magnetic tape controller that can have up to 8 attached tape drives.  Note that this is internally treated as a disk drive so use the disk command to control it, see \cite{TM11}.
\end{description}

%--------------------------------------------------------------------------------------------------
\section{Clock}
The main function of the clock device is to provide a periodic interrupt.
It can be enabled or disabled and the time between interrupts can be set in $\frac{1}{10}$th of a second.
The base time can be changed by editing the code.
Note that the timing is only approximate and depends on many things.
Also since the simulation is not running at the same speed as actual hardware the number of instructions between each interrupt will be different from actual hardware.
The clock device adds only two procedures to the basic I/O device API.

This must be done before using the device.
It starts the task that produces the periodic interrupts.
\begin{lstlisting}
procedure init(self : in out clock_device; ptr : clock_access);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The clock object to access.
  \item \keyword{ptr} - An access to the clock object.  It is passed to the clock task.  This must be the same clock object.
\end{itemize}

Set the number of ticks per second as the base interval rate.
(default value is 10 ticks per second).
This will apply to all clock objects.
This should be called, if needed, before the simulated software tries to initialize the clock.
\begin{lstlisting}
procedure setBaseRate(b : Duration);
\end{lstlisting}
\begin{itemize}
  \item \keyword{b} - The number of expected ticks per second as a \datatype{Duration}.
\end{itemize}

\subsection{KW11 Clock}
While the KW11 is based on the clock I/O device, it extends the interface in a slightly different way.
Instead of the base rate being the number of ticks per second and the host software being able to set the number of ticks per interrupt, the rate is defined as number of ticks per second.
The \datatype{rate} enumeration is defined as:
\begin{itemize}
  \item \keyword{Hz60} - Interrupt at a 60Hz rate (this is standard).
  \item \keyword{Hz50} - Interrupt at a 50Hz rate (this is a hardware selectable option).
  \item \keyword{Hz1} - Interrupt at a 1Hz rate (used for systems that can't keep up with faster clocks such as Pi-Mainframe).
  \item \keyword{Hz0\_1} - Interrupt at a 0.1Hz rate (used for systems that can't keep up with faster clocks such as Pi-Mainframe).
\end{itemize}

This must be done before using the device.
Unlike the clock object, this can set the clock rate at initialization.
\begin{lstlisting}
procedure init(self : in out kw11; ptr : kw11_access; r : rate);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The KW11 object to access.
  \item \keyword{ptr} - An access to the KW11 object.  It is passed to the clock task.  This must be the same clock object.
  \item \keyword{r} - The rate for the KW11 to produce interrupts
\end{itemize}

Set the interrupt rate to either 60Hz or 50Hz.
\begin{lstlisting}
procedure setBaseRate(self : in out kw11; r : rate);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The KW11 object to access.
  \item \keyword{r} - The number of expected interrupts per second as a \datatype{rate}.
\end{itemize}

%--------------------------------------------------------------------------------------------------
\section{Disk Interfaces}
The initial \verb|disk_ctrl| device was designed with 8 inch floppy disks in mind for use by CP/M.
It has been extended to allow other disk geometries, but follows the model of tracks, sectors per track, and heads for defining a disk.
All data transfers occur between the request and the next instruction processed by the simulator.
The disk's data storage is an unformatted binary file with the location in the file being computed based on the disk's geometry and the requested track, sector, and head.
A disk controller may control multiple simulated disk drives.

Additional disk controllers based on actual hardware have been developed to support the PDP-11 CPU.
Enough of the hardware has been simulated to support the RT-11 operating system.
The controllers supported here are RK11 and RK611.
The TM11 tape controller is also internally treated as a disk drive.
This may change in the future.

The common disk definitions are a record for the disk geometry:
\begin{lstlisting}
type geometry is record
   tracks  : word;     --  Number of tracks on disk
   sectors : word;     --  Number of sectors per track
   heads   : byte;     --  Number of heads per drive (currently unused)
end record;
\end{lstlisting}
With constants defined for 8 inch floppy disks (\constant{floppy8\_geom}), a hard disk (\constant{hd\_geom}), and a null value (\constant{null\_geom}).  Other disk controllers may define their own geometries.

The common API is:

Close the attached file
\begin{lstlisting}
procedure close(self : in out disk_ctrl; drive : byte) is null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The disk object to access.
\end{itemize}

Get the name of the attached file, if any.
\begin{lstlisting}
function fname(self : in out disk_ctrl; drive : byte)
         return String is ("");
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The disk object to access.
  \item \keyword{drive} - The drive number for the attached file.
\end{itemize}

Get geometry for drive.
\begin{lstlisting}
function getGeometry(self : in out disk_ctrl; drive : byte)
         return geometry is (null_geom);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The disk object to access.
  \item \keyword{drive} - The drive number for the attached file.
\end{itemize}

Return maximum drive number.
\begin{lstlisting}
function max_drive(self : in out disk_ctrl)
         return byte is (0);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The disk object to access.
\end{itemize}

Open the attached file for a disk drive.
If the file does not exist, it is created and extended to a size based on the geometry.
If the file exists, it will be opened, even if it is not the full size implied by the geometry.
\begin{lstlisting}
procedure open(self : in out disk_ctrl; drive : byte;
               geom : geometry; name : String) is null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The disk object to access.
  \item \keyword{drive} - The drive number for the attached file.
  \item \keyword{geom} - The geometry of the attached file.
  \item \keyword{name} - The name of the file to open.
\end{itemize}

Is a file attached to the specified drive?
\begin{lstlisting}
function present(self : in out disk_ctrl; drive : byte)
         return Boolean is (False);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The disk object to access.
  \item \keyword{drive} - The drive number for the attached file.
\end{itemize}

Is the specified drive read-only?
\begin{lstlisting}
function readonly(self : in out disk_ctrl; drive : byte)
         return Boolean is (True);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The disk object to access.
  \item \keyword{drive} - The drive number for the attached file.
\end{itemize}

Set the specified drive's read-only state.
\begin{lstlisting}
procedure readonly(self : in out disk_ctrl; drive : byte;
                  state : Boolean) is null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The disk object to access.
  \item \keyword{drive} - The drive number for the attached file.
  \item \keyword{state} - True for the drive to be set as read-only.
\end{itemize}

Set geometry for drive.  In most cases, this is done when opening the attached file.
\begin{lstlisting}
procedure setGeometry(self : in out disk_ctrl; drive : byte;
                      geom : geometry) is null;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The disk object to access.
  \item \keyword{drive} - The drive number for the attached file.
  \item \keyword{geom} - The geometry of the attached file.
\end{itemize}

The current disk drive implementations (\package{BBS.Sim\_CPU.io.disk.floppy}, \package{BBS.Sim\_CPU.io.disk.rk11}, and \package{BBS.Sim\_CPU.io.disk.rk611}) all use the same API, with only additional geometries being defined.

%--------------------------------------------------------------------------------------------------
\section{Serial Ports}
Serial ports provide a way to provide a terminal interface to a simulator.
There are no API extensions generic to serial ports.
Each type of serial device has its own extensions as necessary.

\subsection{Print8}
This is about the simplest use for a serial port.
It simulates a printer.
Since output is buffered by the host system, the simulation sees it complete instantly and there are no interrupts.
Output is sent to a file, which may be an actual printer.
The added API is for dealing with the output file.

Close the attached file.
\begin{lstlisting}
procedure close(self : in out print8);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The print8 object to access.
\end{itemize}

Get the name of the attached file, if any.
\begin{lstlisting}
function fname(self : in out print8) return String;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The print8 object to access.
\end{itemize}

Open the attached file.
\begin{lstlisting}
procedure open(self : in out print8; name : String);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The print8 object to access.
  \item \keyword{name} - The name of the file to open.
\end{itemize}

Get the presence of the attached file, if any.
\begin{lstlisting}
function present(self : in out print8) return Boolean;
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The print8 object to access.
\end{itemize}

\subsection{Single Line Telnet Port}
The single line telnet port provides a  basic serial port.
This device has an \verb|init| routine that is used to set the TCP port.
Using \verb|telnet| to connect to this port connects to the device and will allow communication with software running on the simulator.
If enabled, this device can provide interrupts to the simulator when characters are received.

Set device port and do the network initialization.
This must be done before using the device.
\begin{lstlisting}
procedure init(self : in out tel_tty; ptr : telnet_access;
               port : GNAT.Sockets.Port_Type);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The terminal object to access.
  \item \keyword{ptr} - An access value for a terminal object.  It should point to \keyword{self}.
  \item \keyword{port} - The port number to use.
\end{itemize}

\subsection{Multi-Line Telnet Port}
The multi-line telnet port combines 8 ports into a single simulated device.
The \verb|init| routine specifies the starting TCP port.
It and the next seven ports can be accessed by \verb|telnet| to communicate with software running on the simulator.
This uses a single exception and requires fewer addresses than having 8 single line telnet ports.

Set device port and do the network initialization.
This must be done before using the device.
\begin{lstlisting}
procedure init(self : in out mux_tty; ptr : mux_access;
               port : GNAT.Sockets.Port_Type);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The terminal multiplexer object to access.
  \item \keyword{ptr} - An access value for a terminal multiplexer object.  It should point to \keyword{self}.
  \item \keyword{port} - The port number to use.
\end{itemize}

\subsection{DL11 Single Line Terminal Interface}
The DL11 provides a basic serial port.
This device has an \verb|init| routine that is used to set the TCP port.
Using \verb|telnet| to connect to this port connects to the device and will allow communication with software running on the simulator.
Since this device simulates an actual physical interface, it is capable of providing interrupts when a character is received or when a character is transmitted.

Set device port and do the network initialization.
This must be done before using the device.
\begin{lstlisting}
procedure init(self : in out dl11x; ptr : dl11_access;
               port : GNAT.Sockets.Port_Type);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The DL11 object to access.
  \item \keyword{ptr} - An access value for a DL11 object.  It should point to \keyword{self}.
  \item \keyword{port} - The port number to use.
\end{itemize}

\subsection{DZ11 8 Line Terminal Interface}
The DZ11 combines 8 ports into a single simulated device.
This device has an \verb|init| routine that is used to set the TCP port.
Using \verb|telnet| to connect to this port connects to the device and will allow communication with software running on the simulator.
Since this device simulates an actual physical interface, it is capable of providing interrupts when a character is received or when a character is transmitted.

Set device port and do the network initialization.
This must be done before using the device.
\begin{lstlisting}
procedure init(self : in out DZ11; ptr : DZ11_access;
               port : GNAT.Sockets.Port_Type);
\end{lstlisting}
\begin{itemize}
  \item \keyword{self} - The DZ11 object to access.
  \item \keyword{ptr} - An access value for a DZ11 object.  It should point to \keyword{self}.
  \item \keyword{port} - The port number to use.
\end{itemize}


%--------------------------------------------------------------------------------------------------
\section{Tape}
From the point of view of the host program, the paper tape reads bytes and writes bytes.
The read bytes come from an attached file and the written bytes are written to an attached file (note that the should be two different files).
One could also imagine this as a cassette tape interface if that better fits the computer you're simulating.

%========================================================
\chapter{Running a Simulation}
The \function{start} procedure is called first to specify a starting address for program execution.  This is called to start simulator execution at a specific address.  This is made null rather than abstract so that simulators that don't use it don't need to override it.
\begin{lstlisting}
procedure start(self : in out simulator; addr : addr_bus) is null;
\end{lstlisting}
Then, each instruction is individually executed using the \function{run} procedure.  This is called once per frame when start/stop is in the start position and run/pause is in the run position.
\begin{lstlisting}
procedure run(self : in out simulator) is abstract;
\end{lstlisting}
Certain conditions or instruction can cause a simulator to halt.
The \function{halted} and \function{continue\_proc} routines can be used to test for this condition and clear it.

Check if simulator is halted.  If a simulator doesn't define this, it will return \constant{False}.
\begin{lstlisting}
function halted(self : in out simulator) return Boolean is (False);
\end{lstlisting}
This clears the halted flag allowing processing to continue.  If a simulator doesn't define this, it has no action.
\begin{lstlisting}
procedure continue_proc(self : in out simulator) is null;
\end{lstlisting}
From all of this, one can write the core of a simulator as follows:
\begin{lstlisting}
--
--  A bunch of other stuff including defining some
--  simulator as "sim"
--
begin
   sim.init;
   --
   --  I/O devices can be added here...
   --
   sim.load("image.hex");
   sim.start(0);  --  Start execution at address 0
   while not sim.halted loop
      sim.run;
   end loop;
end
\end{lstlisting}

%========================================================
\clearpage
\addcontentsline{toc}{chapter}{Bibleography}
\nocite{dec84}
\bibliographystyle{plain}
\bibliography{sim.bib}

\end{document}

