*
*  Test program for LEA instructions.
*
    ORG $1000

START:  ; First instruction of program
    LEA START,A0
    LEA (A0),A1
    LEA 2(A1),A2
    LEA START(PC),A3
    ADDQ #2,D0
    LEA 2(A1,D0),A4
    LEA START(PC,D0),A5
*
    END START
